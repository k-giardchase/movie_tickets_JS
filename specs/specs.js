describe("", function() {
  describe("", function() {
    it("", function() {
      var testFunction = Object.create(Function);
      testBankAccount.withdraw(50);
      expect(test).to.equal(-50);
    });
  });
});
